cd bolt/isaacgym

conda activate rlgpu

export LD_LIBRARY_PATH=/home/maxime/anaconda3/envs/rlgpu/lib:$LD_LIBRARY_PATH

echo $PATH

cd python/examples
python joint_monkey.py

python train.py task=Cartpole

tensorboard --logdir=runs --host localhost --port 8088
navigate in browser to http://localhost:8088

https://github.com/Gepetto/example-robot-data


python train.py task=Cartpole checkpoint=runs/Cartpole_30-18-18-09/nn/Cartpole.pth test=True


si changement de num_envs (dans les fichiers ou en commmande) attention au "assert(self.batch_size % self.minibatch_size == 0)", il faut changer la taille du minibatch aussi and le fichier cgf training ie BoltPPO.yaml
